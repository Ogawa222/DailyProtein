dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon
        // 追記
        maven {
            url "https://jitpack.io"
        }
    }
}
rootProject.name = "DailyProtein"
include ':app'
